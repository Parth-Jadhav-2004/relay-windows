# Classic PAT with `repo` scope, or a fine-grained token that can read this private repo.
# Copy to `%APPDATA%\Tinycast\com.tinycast.windows\.env` (Release) or
# `%APPDATA%\Tinycast\com.tinycast.windows.dev\.env` (Debug). Do not commit the copy.
TINYCAST_GITHUB_TOKEN=
