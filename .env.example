# Classic PAT with `repo` scope, or a fine-grained token that can read this private repo.
# Copy to `%APPDATA%\Relay\com.relay.windows\.env` (Release) or
# `%APPDATA%\Relay\com.relay.windows.dev\.env` (Debug). Do not commit the copy.
RELAY_GITHUB_TOKEN=
